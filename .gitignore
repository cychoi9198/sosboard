# Not part of this project — other unrelated local projects living in the same XAMPP htdocs.
/새 폴더/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# SQLite runtime database (created locally when driver=sqlite; never committed)
/var/data/*.sqlite
/var/data/*.sqlite-wal
/var/data/*.sqlite-shm
